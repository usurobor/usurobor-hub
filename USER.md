# USER.md (moved)

The primary human spec now lives at `spec/USER.md`.

OpenClaw still looks for `USER.md` at the workspace root; this stub preserves that
contract while directing readers to the canonical location.
