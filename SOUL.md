# SOUL.md (moved)

This agent's soul spec now lives at `spec/SOUL.md`.

OpenClaw currently expects `SOUL.md` at the workspace root; this stub exists so the
agent can still start while pointing humans and other agents at the canonical file.
